import { useDispatch, useSelector } from 'react-redux';

// Remove TypeScript annotations and simplify the exports
export const useAppDispatch = useDispatch;
export const useAppSelector = useSelector; 